name: Test Simple Workflow

on:
  push:
    branches:
      - feat/issue-631-document-integration-reorganization
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Echo test
        run: |
          echo "Test workflow is working!"
          echo "Branch: ${{ github.ref }}"
          echo "Event: ${{ github.event_name }}"